const initialState = {};


const PartnerDataDuck = (state = initialState, {type, payload}) => {

    switch (type) {

        default:
            return state;
    }
};

export default PartnerDataDuck;